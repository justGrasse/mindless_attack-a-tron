<div class='card-container'>
  <div class='card-type-container'>
    <h4>creatures (<%= deck.cards_by_type('Creature').count %>):</h4>
    <ul class='card-list'>
    <% deck.cards_by_type('Creature').each do |card| %>
      <li class='card-name'><a href="/cards/<%= card.id %>"><%= card.name %></a></li>
    <% end %>
    </ul>
    <h4>sorceries (<%= deck.cards_by_type('Sorceries').count %>):</h4>
    <ul class='card-list'>
    <% deck.cards_by_type('Sorceries').each do |card| %>
      <li class='card-name'><a href="/cards/<%= card.id %>"><%= card.name %></a></li>
    <% end %>
    </ul>
    <h4>instants (<%= deck.cards_by_type('Instants').count %>):</h4>
    <ul class='card-list'>
    <% deck.cards_by_type('Instants').each do |card| %>
      <li class='card-name'><a href="/cards/<%= card.id %>"><%= card.name %></a></li>
    <% end %>
    </ul>
    <h4>artifacts (<%= deck.cards_by_type('Artifacts').count %>):</h4>
    <ul class='card-list'>
    <% deck.cards_by_type('Artifacts').each do |card| %>
      <li class='card-name'><a href="/cards/<%= card.id %>"><%= card.name %></a></li>
    <% end %>
    </ul>
    <h4>enchantments (<%= deck.cards_by_type('Enchantments').count %>):</h4>
    <ul class='card-list'>
    <% deck.cards_by_type('Enchantments').each do |card| %>
      <li class='card-name'><a href="/cards/<%= card.id %>"><%= card.name %></a></li>
    <% end %>
    </ul>
    <h4>lands (<%= deck.cards_by_type('Lands').count %>):</h4>
    <ul class='card-list'>
    <% deck.cards_by_type('Lands').each do |card| %>
      <li class='card-name'><a href="/cards/<%= card.id %>"><%= card.name %></a></li>
    <% end %>
    </ul>
  </div>
</div>
